[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fcs_control"
version = "0.1.0"
authors=[{name='Bart J. B. Smeets', email='bart.smeets.qsp@kuleuven.be'}]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
